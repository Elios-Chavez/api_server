import type { InventoryAlert } from '@/types/domain'; import { StatusBadge } from '@/components/admin/StatusBadge';
export function AlertList({alerts}:{alerts:InventoryAlert[]}){return <div className="alert-list">{alerts.map((alert)=><article className="alert-list-item" key={alert.id}><div><StatusBadge status={alert.severity}/><h3>{alert.productName}</h3><p>{alert.title}</p>{alert.difference !== 0 && <div className="alert-list-evidence"><span>Digital <strong>{alert.digitalStock}</strong></span><span>Físico <strong>{alert.physicalStock}</strong></span><span>Diferencia <strong>{Math.abs(alert.difference)}</strong></span></div>}</div><span className="alert-list-action">{alert.suggestedAction}</span></article>)}</div>}
