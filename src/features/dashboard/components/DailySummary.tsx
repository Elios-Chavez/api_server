import type { InventoryAlert } from '@/types/domain'; import { severityOrder } from '@/features/alerts/utils/alertRules';
export function DailySummary({alerts}:{alerts:InventoryAlert[]}){const counts=severityOrder.reduce((result,severity)=>({...result,[severity]:alerts.filter((alert)=>alert.severity===severity).length}),{} as Record<string,number>);return <section className="daily-summary"><div className="dashboard-section-heading"><div><p className="section-label">Lectura rápida</p><h2>Resumen del día</h2></div><span>Fuente operativa</span></div><div className="daily-summary-items"><span><strong>{counts.priority}</strong> prioridad</span><span><strong>{counts.attention}</strong> por revisar</span><span><strong>{counts.opportunity}</strong> oportunidad</span><span><strong>{counts.stable}</strong> estables</span></div></section>}
