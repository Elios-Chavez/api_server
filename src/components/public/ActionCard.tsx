import type {ReactNode} from 'react'; import {Link} from 'react-router-dom'; export function ActionCard({title,description,icon,to,featured=false}:{title:string;description:string;icon:ReactNode;to:string;featured?:boolean}){return <Link className={`action-card ${featured?'action-card-featured':''}`} to={to}><span className="action-card-icon">{icon}</span><span><span className="action-card-title">{title}</span><span className="action-card-description">{description}</span></span></Link>}
