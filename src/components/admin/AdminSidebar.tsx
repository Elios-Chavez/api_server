import { LogOut } from 'lucide-react'; import { NavLink, useNavigate } from 'react-router-dom'; import { adminNavigation } from '@/config/navigation'; import { endAdminSession } from '@/features/auth/adminSession'; import { AppLogo } from '../common/AppLogo';
export function AdminSidebar(){const navigate=useNavigate();const logout=()=>{endAdminSession();navigate('/')};return <aside className="admin-sidebar"><AppLogo/><nav aria-label="Navegación administrativa" className="admin-nav">{adminNavigation.map(({label,path,icon:Icon})=><NavLink key={path} to={path} className={({isActive})=>'admin-nav-link '+(isActive?'is-active':'')}><Icon aria-hidden="true" className="size-5"/>{label}</NavLink>)}</nav><button className="admin-logout" onClick={logout} type="button"><LogOut aria-hidden="true" className="size-5"/>Cerrar sesión</button></aside>}
