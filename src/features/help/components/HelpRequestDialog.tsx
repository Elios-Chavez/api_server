import { Check, X } from 'lucide-react';
export function HelpRequestDialog({ onClose }: { onClose: () => void }) { return <div aria-labelledby="help-dialog-title" aria-modal="true" className="summary-backdrop" onClick={onClose} role="dialog"><div className="help-dialog" onClick={(event) => event.stopPropagation()}><button aria-label="Cerrar solicitud de ayuda" className="close-summary" onClick={onClose} type="button"><X aria-hidden="true"/></button><span className="help-confirmation-icon"><Check aria-hidden="true"/></span><h2 id="help-dialog-title">Se ha solicitado apoyo.</h2><p>Un encargado puede ayudarte en este punto.</p><button className="summary-button" onClick={onClose} type="button">Entendido</button></div></div>; }
