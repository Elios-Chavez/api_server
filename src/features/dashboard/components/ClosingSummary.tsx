import type { InventoryAlert } from '@/types/domain';
export function ClosingSummary({alerts}:{alerts:InventoryAlert[]}){const priority=alerts.find((alert)=>alert.severity==="priority");const attention=alerts.find((alert)=>alert.severity==="attention");const focus=priority??attention;return <section className="closing-summary"><p className="section-label">Cierre del día</p><h2>Resumen para cerrar el día</h2>{focus?<><p>{priority?"Hoy se detectó una discrepancia importante":"Hoy hay una existencia próxima al punto de reorden"} en {focus.productName}. Conviene revisar el inventario antes del próximo pedido.</p><span>{focus.suggestedAction}</span></>:<><p>No hay alertas operativas que requieran seguimiento inmediato.</p><span>El inventario observado se mantiene estable.</span></>}</section>}
