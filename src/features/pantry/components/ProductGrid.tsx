import type { Product } from '@/types/domain'; import { ProductCard } from './ProductCard';
export function ProductGrid({ products, quantities, onAdd, onDecrease, onIncrease }: { products: Product[]; quantities: Record<string, number>; onAdd: (id: string) => void; onDecrease: (id: string) => void; onIncrease: (id: string) => void }) { if (!products.length) return <div className="pantry-empty-results"><h3>No encontramos productos</h3><p>Prueba con otra búsqueda o categoría.</p></div>; return <div className="product-grid">{products.map((product) => <ProductCard key={product.id} product={product} quantity={quantities[product.id] ?? 0} onAdd={() => onAdd(product.id)} onDecrease={() => onDecrease(product.id)} onIncrease={() => onIncrease(product.id)}/>)}</div>; }
