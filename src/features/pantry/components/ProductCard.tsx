import { Plus, ShoppingBasket } from 'lucide-react'; import type { Product } from '@/types/domain'; import { QuantityControl } from './QuantityControl';
export function ProductCard({ product, quantity, onAdd, onDecrease, onIncrease }: { product: Product; quantity: number; onAdd: () => void; onDecrease: () => void; onIncrease: () => void }) { return <article className="product-card"><div className="product-placeholder" aria-hidden="true">{product.imageUrl ? <img alt={product.name} loading="lazy" src={product.imageUrl}/> : <ShoppingBasket className="size-9"/>}</div><div className="product-card-copy"><span className="product-category">{product.category}</span><h3>{product.name}</h3><p>{product.price.toFixed(2)} MXN</p></div>{quantity > 0 ? <QuantityControl quantity={quantity} onDecrease={onDecrease} onIncrease={onIncrease}/> : <button className="add-product-button" onClick={onAdd} type="button"><Plus aria-hidden="true" className="size-5"/>Agregar</button>}</article>; }
