import {Link} from 'react-router-dom'; import {Settings} from 'lucide-react'; import {AppLogo} from '../common/AppLogo'; import {appConfig} from '@/config/app'; export function PublicHeader(){return <header className="public-header"><Link aria-label={`Ir al inicio de ${appConfig.appName}`} to="/"><AppLogo/></Link><Link className="admin-link" to="/admin"><Settings aria-hidden="true" className="size-5"/><span>Admin</span></Link></header>}
