import { X } from 'lucide-react'; import type { Product } from '@/types/domain';
export function PantrySummary({ items, quantities, totalItems, onClose, onFinish }: { items: Product[]; quantities: Record<string, number>; totalItems: number; onClose: () => void; onFinish: () => void }) {
  return <div aria-labelledby="pantry-summary-title" aria-modal="true" className="summary-backdrop" onClick={onClose} role="dialog"><div className="summary-dialog" onClick={(event) => event.stopPropagation()}><div className="summary-heading"><div><p className="section-label">Lista local</p><h2 id="pantry-summary-title">Mi despensa</h2></div><button aria-label="Cerrar resumen" className="close-summary" onClick={onClose} type="button"><X aria-hidden="true"/></button></div><div className="summary-list">{items.map((product) => <div key={product.id}><span>{product.name}</span><strong>×{quantities[product.id]}</strong></div>)}</div><p className="summary-total">{totalItems} artículos</p><div className="summary-actions"><button className="secondary-button" onClick={onClose} type="button">Seguir agregando</button><button className="summary-button" onClick={onFinish} type="button">Finalizar lista</button></div></div></div>;
}
