import type { FeedbackMood } from "@/types/domain";
const options: Array<{ value: FeedbackMood; label: string; symbol: string }> = [{ value: "sad", label: "Triste", symbol: "😞" }, { value: "neutral", label: "Neutral", symbol: "😐" }, { value: "happy", label: "Feliz", symbol: "🙂" }];
export function RatingSelector({ value, onChange }: { value: FeedbackMood | ""; onChange: (mood: FeedbackMood) => void }) { return <div aria-label="Valoración de la experiencia" className="rating-selector" role="radiogroup">{options.map((option) => <button aria-checked={value === option.value} aria-label={option.label} className={value === option.value ? "rating-mood is-selected" : "rating-mood"} key={option.value} onClick={() => onChange(option.value)} role="radio" type="button"><span aria-hidden="true">{option.symbol}</span><strong>{option.label}</strong></button>)}</div>; }
